# 左侧目录点击切换地图逻辑

本文整理左侧目录点击后，地图视角/标记如何切换的完整链路，不做代码修改。

## 入口：目录节点点击

左侧目录的节点点击，触发 `onSelectNode(node.id)`。

```249:255:apps/sun/components/TableOfContents.tsx
          {nodes.map((node) => (
            <NodeItem
              key={node.id}
              node={node}
              isActive={node.id === currentNodeId}
              onSelect={() => onSelectNode(node.id)}
            />
          ))}
```

`TableOfContents` 由 `ReaderLayout` 传入 `onSelectNode`，在点击时调用 `selectNode`，并在移动端关闭目录抽屉。

```146:153:apps/sun/components/ReaderLayout.tsx
        <TableOfContents
          work={work}
          currentNodeId={currentNode?.id || null}
          onSelectNode={(nodeId) => {
            selectNode(nodeId)
            setIsTocOpen(false) // 移动端选择后关闭抽屉
          }}
          isLoading={isLoading}
        />
```

## URL 同步：selectNode -> 更新查询参数

`useNarrative.selectNode` 只更新 URL 的 `node` 参数，不直接改 state。

```175:180:apps/sun/hooks/useNarrative.ts
  const selectNode = useCallback(
    (nodeId: string) => {
      updateUrl(nodeId)
    },
    [updateUrl]
  )
```

`updateUrl` 用 `router.replace` 同步查询参数。

```66:72:apps/sun/hooks/useNarrative.ts
  const updateUrl = useCallback(
    (nodeId: string) => {
      const params = new URLSearchParams(searchParams.toString())
      params.set('node', nodeId)
      router.replace(`?${params.toString()}`, { scroll: false })
    },
    [router, searchParams]
  )
```

## 载入节点：URL 变化 -> currentNode/nextNode

当 `nodeIdFromUrl` 变化时加载节点与下一节点，并加载该章节的节点列表（用于目录高亮）。

```132:172:apps/sun/hooks/useNarrative.ts
  useEffect(() => {
    if (!nodeIdFromUrl) return

    async function loadNode() {
      try {
        setIsLoading(true)

        const node = await getNodeById(nodeIdFromUrl)
        if (!node) { /* ... */ return }

        setCurrentNode(node)

        if (node.links?.next) {
          const next = await getNodeById(node.links.next)
          setNextNode(next || null)
        } else {
          setNextNode(null)
        }

        const { nodes } = await loadChapterNodes(node.volume, node.chapter)
        setChapterNodes(nodes)
        setIsLoading(false)
      } catch (err) { /* ... */ }
    }

    loadNode()
  }, [nodeIdFromUrl])
```

## 计算地图场景：currentNode -> mapScene

`ReaderLayout` 基于 `currentNode/places/nextNode` 计算 `mapScene`，传给地图组件。

```31:39:apps/sun/components/ReaderLayout.tsx
  const mapScene = useMapScene(currentNode, places, nextNode)

  const routeRequest = useMemo<RouteRequest | null>(() => {
    if (!mapScene?.route) return null
    return {
      fromId: mapScene.route.fromId,
      toId: mapScene.route.toId,
      fromCoord: mapScene.route.fromCoord,
      toCoord: mapScene.route.toCoord,
    }
  }, [mapScene?.route])
```

```92:100:apps/sun/components/ReaderLayout.tsx
      <div className="absolute inset-0 z-0">
        <DynamicMapViewer
          className="cesium-viewer"
          scene={mapScene}
          routeData={routeData}
          highlightedPlaceId={highlightedPlaceId}
          onMarkerClick={handleMarkerClick}
          onMarkerHover={handleMarkerHover}
        />
      </div>
```

## 地图渲染：scene 变化 -> 重新飞行

`DynamicMapViewer` 只做动态导入，实际逻辑在 `MapViewer`。

```16:21:apps/sun/components/DynamicMapViewer.tsx
export const DynamicMapViewer = dynamic<DynamicMapViewerProps>(
  () => import('./MapViewer'),
  {
    ssr: false,
    loading: () => (/* ... */),
  }
)
```

`MapViewer` 监听 `scene` 变化：

- 若 `scene.sceneId` 未变：仅重建标记并更新高亮
- 若 `scene.sceneId` 变化：清除旧标记/路线，添加新标记，调用 `flyToCamera(scene.camera)` 飞到新视角

```420:455:apps/sun/components/MapViewer.tsx
  useEffect(() => {
    if (!scene || !viewerRef.current) return

    if (scene.sceneId === lastSceneIdRef.current) {
      // 仅更新高亮状态
      clearMarkers()
      scene.markers.forEach((marker) => {
        const isHighlighted = highlightedPlaceId === marker.id
        const entity = addMarker(marker, isHighlighted)
        if (entity) {
          markersRef.current.set(marker.id, entity)
        }
      })
      return
    }

    lastSceneIdRef.current = scene.sceneId

    clearMarkers()
    clearRoute()

    scene.markers.forEach((marker) => {
      const isHighlighted = highlightedPlaceId === marker.id
      const entity = addMarker(marker, isHighlighted)
      if (entity) {
        markersRef.current.set(marker.id, entity)
      }
    })

    // 飞到目标位置
    flyToCamera(scene.camera)
  }, [scene, highlightedPlaceId, clearMarkers, clearRoute, addMarker, flyToCamera])
```

## 地图场景数据来源

`useMapScene` 根据 `currentNode.map` 生成 `markers` 与 `camera`。

- `preset`：使用节点配置（如果 `height` 缺省，则用默认值）
- `autoFit`：根据标记点计算 `bounds`，使用 `padding` 与 `durationMs`
- 路线：优先用节点 `map.route`，否则跨章节切换时自动生成

```96:147:apps/sun/hooks/useMapScene.ts
    if (map.camera.mode === 'preset') {
      camera = {
        mode: 'preset',
        lng: map.camera.lng,
        lat: map.camera.lat,
        height: map.camera.height || 10000,
        heading: map.camera.heading || 0,
        pitch: map.camera.pitch || -45,
        durationMs: map.camera.durationMs || 1200,
      }
    } else {
      if (markers.length === 0) {
        camera = {
          mode: 'preset',
          lng: 112.5,
          lat: 27.9,
          height: 50000,
          heading: 0,
          pitch: -60,
          durationMs: map.camera.durationMs || 1200,
        }
      } else if (markers.length === 1) {
        camera = {
          mode: 'preset',
          lng: marker.lng,
          lat: marker.lat,
          height: 10000,
          heading: 0,
          pitch: -45,
          durationMs: map.camera.durationMs || 1200,
        }
      } else {
        camera = {
          mode: 'autoFit',
          bounds,
          padding: map.camera.padding || 0.25,
          durationMs: map.camera.durationMs || 1200,
        }
      }
    }
```

## 总结链路

1. 目录点击节点 -> `onSelectNode(nodeId)`
2. `selectNode` 更新 URL 查询参数 `node`
3. `useNarrative` 监听 URL 变化，加载 `currentNode/nextNode`
4. `useMapScene` 根据节点生成 `mapScene`
5. `MapViewer` 收到新 `scene` 后清理旧状态并 `flyToCamera`


